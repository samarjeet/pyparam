package:
  name: pyparam
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - pyparam

about:
  home: add_url_here
  license: MIT License
